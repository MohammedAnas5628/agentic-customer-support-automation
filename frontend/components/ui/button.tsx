import Link from "next/link";
import { cn } from "@/lib/utils";
type Props = { children: React.ReactNode; className?: string; href?: string; variant?: "primary" | "secondary" | "ghost"; onClick?: () => void; type?: "button" | "submit"; disabled?: boolean };
export function Button({ children, className, href, variant = "primary", onClick, type = "button", disabled }: Props) { const styles = cn("focus-ring inline-flex items-center justify-center gap-2 rounded-full px-5 py-3 text-sm font-semibold transition duration-300 disabled:opacity-50 disabled:cursor-not-allowed disabled:hover:translate-y-0", variant === "primary" && "bg-ink text-white hover:-translate-y-0.5 hover:bg-electric hover:shadow-glow", variant === "secondary" && "bg-white text-ink ring-1 ring-slate-200 hover:bg-slate-50", variant === "ghost" && "text-slate-600 hover:bg-slate-100", className); return href ? <Link className={styles} href={href}>{children}</Link> : <button suppressHydrationWarning className={styles} onClick={onClick} type={type} disabled={disabled}>{children}</button>; }
