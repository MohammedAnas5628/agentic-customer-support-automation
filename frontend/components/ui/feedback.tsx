"use client";
import { createContext,useContext,useState } from "react"; import { CheckCircle2,X } from "lucide-react";
type Toast={message:string;tone:"success"|"error"};const ToastContext=createContext<{show:(toast:Toast)=>void}|undefined>(undefined);
export function FeedbackProvider({children}:{children:React.ReactNode}){const[toast,setToast]=useState<Toast>();const show=(value:Toast)=>{setToast(value);window.setTimeout(()=>setToast(undefined),4500)};return <ToastContext.Provider value={{show}}>{children}{toast&&<div role="status" className={`fixed bottom-5 right-5 z-[70] flex max-w-sm items-center gap-3 rounded-2xl px-4 py-3 text-sm font-semibold shadow-soft ${toast.tone==="success"?"bg-ink text-white":"bg-rose-600 text-white"}`}><CheckCircle2 size={18}/><span>{toast.message}</span><button onClick={()=>setToast(undefined)} aria-label="Dismiss" className="ml-2"><X size={16}/></button></div>}</ToastContext.Provider>}
export function useFeedback(){const context=useContext(ToastContext);if(!context)throw new Error("useFeedback must be used within FeedbackProvider");return context}
export function ConfirmDialog({open,title,description,onConfirm,onClose,busy}:{open:boolean;title:string;description:string;onConfirm:()=>void;onClose:()=>void;busy?:boolean}){if(!open)return null;return <div role="dialog" aria-modal="true" aria-labelledby="dialog-title" className="fixed inset-0 z-[60] grid place-items-center bg-ink/40 p-5 backdrop-blur-sm"><div className="w-full max-w-md rounded-3xl bg-white p-6 shadow-soft"><h2 id="dialog-title" className="text-xl font-extrabold">{title}</h2><p className="mt-3 text-sm leading-6 text-slate-500">{description}</p><div className="mt-7 flex justify-end gap-3"><button onClick={onClose} disabled={busy} className="focus-ring rounded-full px-4 py-2 text-sm font-bold">Keep order</button><button onClick={onConfirm} disabled={busy} className="focus-ring rounded-full bg-rose-600 px-4 py-2 text-sm font-bold text-white disabled:opacity-50">{busy?"Cancelling…":"Cancel order"}</button></div></div></div>}
