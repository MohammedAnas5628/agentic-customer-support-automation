import Image from "next/image";
import { ArrowRight, Camera, Headphones, Laptop, MessageCircleHeart, PackageCheck, ShieldCheck, Smartphone, Tv, Watch, WashingMachine, Cable } from "lucide-react";
import { Button } from "@/components/ui/button";
import { catalogCategories, getCategoryImage } from "@/lib/catalog";
const categories = catalogCategories.map((category, index) => ({ ...category, icon: [Laptop, Smartphone, Headphones, Tv, Camera, Watch, WashingMachine, Cable][index], tint: ["bg-indigo-100", "bg-cyan-100", "bg-orange-100", "bg-rose-100", "bg-amber-100", "bg-violet-100", "bg-emerald-100", "bg-slate-100"][index] }));
export function Categories() { return <section className="shell mt-20"><div className="flex items-end justify-between"><div><p className="eyebrow">Find your focus</p><h2 className="mt-2 text-3xl font-extrabold tracking-tight">Explore by category</h2></div><Button href="/products" variant="ghost" className="hidden sm:inline-flex">View all <ArrowRight size={16} /></Button></div><div className="mt-8 grid grid-cols-2 gap-4 md:grid-cols-4">{categories.map(({ name, description, icon: Icon, tint }) => <Button href={`/products?category=${encodeURIComponent(name)}`} variant="secondary" key={name} className="group h-auto min-h-48 flex-col items-stretch justify-between overflow-hidden rounded-3xl p-0 text-left hover:shadow-soft"><div className="relative h-28 overflow-hidden"><Image src={getCategoryImage(name)} alt="" fill sizes="(max-width: 768px) 50vw, 25vw" className="object-cover transition duration-500 group-hover:scale-105" /><span className={`absolute right-4 top-4 grid h-10 w-10 place-items-center rounded-2xl ${tint}`}><Icon size={20} /></span></div><div className="p-5"><span className="text-base font-bold">{name}</span><span className="mt-1 block text-xs leading-5 text-slate-500">{description}</span></div></Button>)}</div></section> }
export function SupportAndPreview() { return <section className="shell mt-20 grid gap-6 lg:grid-cols-[1.15fr_.85fr]"><div className="rounded-4xl bg-gradient-to-br from-electric to-indigo-800 p-8 text-white sm:p-10"><MessageCircleHeart className="text-cyan" /><p className="eyebrow mt-6 text-cyan">Always on your side</p><h2 className="mt-2 max-w-md text-3xl font-extrabold tracking-tight">An expert answer is one question away.</h2><p className="mt-4 max-w-lg leading-7 text-indigo-100">Find product guidance, delivery updates, and care advice with ElectroMart Support.</p><Button href="/support" className="mt-8 bg-white text-ink hover:bg-cyan">Get support <ArrowRight size={16} /></Button></div><div className="surface p-7 sm:p-8"><p className="eyebrow">Your ElectroMart</p><h2 className="mt-2 text-2xl font-extrabold">Everything in one place.</h2><div className="mt-7 grid gap-4">{[[PackageCheck, "Recent orders", "Track each delivery with clarity."], [ShieldCheck, "Care & warranty", "Protection that keeps pace with you."]].map(([Icon, title, text]) => { const Component = Icon as typeof PackageCheck; return <div className="flex gap-4" key={title as string}><span className="grid h-11 w-11 shrink-0 place-items-center rounded-2xl bg-slate-100 text-electric"><Component size={21} /></span><div><p className="font-bold">{title as string}</p><p className="mt-1 text-sm text-slate-500">{text as string}</p></div></div>; })}</div></div></section> }
